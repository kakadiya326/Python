#To attach index to the list data enumerates() will use
k=['a','b','c','d','e']
print(list(enumerate(k))) #it will return list tuple with numeric index
print(dict(enumerate(k))) #it will return dictionary of numeric index